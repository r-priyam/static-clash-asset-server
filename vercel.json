{
    "version": 2,
    "builds": [
        {
            "src": "./api/index.mjs",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api"
        }
    ]
}